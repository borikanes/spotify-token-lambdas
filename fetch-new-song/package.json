{
  "name": "fetch-new-song",
  "version": "1.0.0",
  "description": "Handles the core mechanism to determine when there's a new playlist",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "zip -r songUpdaterFetchNewSong.zip index.js",
    "deploy": "aws lambda update-function-code --function-name songUpdaterFetchNewSong --zip-file fileb://songUpdaterFetchNewSong.zip"
  },
  "author": "Bori Oludemi",
  "license": "ISC",
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  }
}
