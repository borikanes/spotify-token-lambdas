{
  "name": "spotify-refresh-token-lambda",
  "version": "0.0.1",
  "description": "Spotify Refresh token lambda",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "zip -r Archive index.js node_modules/ package-lock.json package.json && aws lambda update-function-code --function-name spotifyRefreshTokenQA --zip-file fileb://Archive.zip --profile borikanes"
  },
  "dependencies": {
    "btoa": "^1.1.2"
  },
  "author": "Bori Oludemi",
  "license": "ISC"
}
